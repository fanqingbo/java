package com.ppcredit.base.collection.set;

/**
 * set
 * HashSet 底层数据结构是hash表，依赖hashCode()和equals（）
 * LinkedHashSet 底层数据结构由链表和哈希表组成，链表保证元素有序，哈希表保证元素唯一
 * TreeSet  底层是红黑树
 */
public class SetClass {
}
